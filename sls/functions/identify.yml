handler: dist/identify.handler
memorySize: 1024
timeout: 29
package:
  patterns:
    - "!**"
    - "dist/identify.mjs"
events:
  - httpApi:
      path: /identify
      method: POST
      authorizer:
        name: cognitoJwt
